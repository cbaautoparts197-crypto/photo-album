# 电子相册系统 - RBS Auto Parts

Vue 3 + Express 5 + SQLite + 七牛云存储

## 开发

```bash
# 前端
cd client && npm install && npm run dev

# 后端
cd server && npm install && npm run dev
```

## 部署

- **前端**: Vercel（`client/` 目录）
- **后端**: Render（`server/` 目录）
- **环境变量**: 见各目录 `.env.example`
